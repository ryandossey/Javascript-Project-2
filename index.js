'use strict';

// optionally add a parameter for how large a board to create
function createBoard() {
    // reminder: each number button should store the value as a data- property
}

function guessNumber(event) {} // takes an event from the number being clicked on

function tryAgain() {} // connected to a try again / replay button

function init() {
    createBoard();
    // add event listeners here
}
init();
